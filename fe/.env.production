# Used by `vite build` (Vercel). Ensures the client targets the real API even if
# Vercel project env is missing for a given environment. Override in Vercel if the API host changes.
VITE_BACKEND_URL=https://movietime-u9aa.onrender.com
